{
  "name": "chat-app-assignment",
  "version": "1.0.0",
  "description": "Chat App assignmnet for wordleCup.io",
  "main": "server/src/server.ts",
  "scripts": {
    "start": "cd server && npm start",
    "develop": "concurrently \"cd server && npm start\" \"cd client && npm run dev\"",
    "install": "cd server && npm i && cd ../client && npm i --production=false",
    "build": "cd client && npm run build",
    "render-build": "npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/spy-adi/chat-app-assignment.git"
  },
  "author": "Aditya Mishra",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/spy-adi/chat-app-assignment/issues"
  },
  "homepage": "https://github.com/spy-adi/chat-app-assignment#readme",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
